name: plan-mode
version: "0.1.5"
description: "Enforced per-session plan mode for Hermes CLI, gateway, and TUI tool dispatch."
author: 100yenadmin
kind: standalone
requires_hermes: ">=0.21.2"
provides_tools: []
provides_hooks:
  - pre_tool_call
  - pre_llm_call
  - on_session_finalize
  - on_session_reset
